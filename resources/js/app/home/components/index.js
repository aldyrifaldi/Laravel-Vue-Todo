import Vue from "vue";

export const Home = Vue.component('home',require('./Home.vue').default)
export const CompletedTasks = Vue.component('compeletedTasks',require('./CompletedTasks.vue').default)

